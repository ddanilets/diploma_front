const browsers = [
  'defaults, firefox >= 45, safari >= 9, iOS >= 9',
  // 'last 3 major versions',  // uncomment, when https://github.com/danethurber/webpack-manifest-plugin/issues/147 would be resolved
];

export default browsers;
